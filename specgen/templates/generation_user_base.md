Generate the {{artifact}} artifact for spec "{{spec_id}}" ({{spec_name}}), against the code in {{root}}.

PRD:

{{prd}}
{{landscape_block}}{{prior_block}}{{language_block}}
Every ID you reference must come from the PRD or from the artifacts above. Your submission is validated against the format's schema and every cross-file rule decidable at this point, before it is written; a violation comes back to you naming the rule that failed, and you correct it and submit again.
