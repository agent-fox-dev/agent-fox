name: agentfox sandbox containers build

on:
  #push:
  #  tags:
  #    - 'v*'
  workflow_dispatch:

jobs:
  build-sandbox-container:
    uses: ./.github/workflows/inc-build.yaml
    with:
      container_name: sandbox
    secrets: inherit
    permissions:
      packages: write
      contents: read
      id-token: write
      attestations: write